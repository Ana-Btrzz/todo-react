{
  "homepage": "https://github.com/Ana-Btrzz/todo-react",
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
}
}
